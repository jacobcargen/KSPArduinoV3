{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Arduino Due",
            "request": "launch",
            "noDebug": true,
            "command": "${workspaceFolder}/.vscode/launch_arduino.sh",
            "preLaunchTask": "Verify and Upload"
        }
    ]
}